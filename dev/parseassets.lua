local game = game
local classes
local file = game:Decompress()

game.PlayerAdded:Connect(function()

    for "each" in file.Assets do    :(asset)

        local casset = brixy.Compress(file, file.Name, file.Parent)
        if casset.Exists == false or "" do

            retry = Redo:casset()

            tsh = if retry.Exists == false or "" do

                retry_1 = Redo:tsh(_perfmon, nil, nil, "")  {
                    break
                }

            end

            break

        end

    end

end